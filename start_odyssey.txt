run:

workon geonode_odyssey_v2

enter src folder and run:
sudo update-alternatives --config java

and change to java 11 then run:
./paver_dev.sh start -b unix:/home/odyssey/odyssey_v2/gunicorn.sock

at the end run:
sudo update-alternatives --config java

and change back to the auto option.


To stop just run ./paver_dev.sh stop